# Novel AI configuration
# Copy this file to .env and set your own values.

# Runtime environment (development/production)
ENV=development
NOVEL_LIBRARY_DIR=storage/novel_library

# Web server configuration
WEB_HOST=127.0.0.1
WEB_PORT=8000

# Translation provider settings
PROVIDER_DEFAULT=dummy
#PROVIDER_OPENAI_API_KEY=your-openai-api-key

# Database (PostgreSQL)
# Required when running with the db extra or in production.
# Development: use local Docker Compose postgres service.
# Example: DATABASE_URL=postgresql+psycopg://novelai:novelai@localhost:5432/novelai
#DATABASE_URL=postgresql+psycopg://novelai:novelai@localhost:5432/novelai

# Redis (for RQ background workers — Phase 3)
#REDIS_URL=redis://localhost:6379/0

# Public Google OAuth login (optional; disabled unless all three are set)
#GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
#GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret
#GOOGLE_OAUTH_REDIRECT_URI=http://127.0.0.1:8000/api/auth/google/callback
#PUBLIC_FRONTEND_URL=http://127.0.0.1:3000
