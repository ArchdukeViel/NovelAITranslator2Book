{
  "adminCredentials": {
    "user": "cbadmin"
  },
  "connections": [
    {
      "id": "dokushodo-reader-readonly",
      "name": "PostgreSQL@db-reader",
      "description": "Dedicated read-only analyst connection profile using least-privilege role novelai_reader",
      "driver": "postgres-jdbc",
      "template": true,
      "readOnly": true,
      "configuration": {
        "host": "db",
        "port": "5432",
        "databaseName": "dokushodo",
        "url": "jdbc:postgresql://db:5432/dokushodo?sslmode=prefer",
        "authModel": "native",
        "authProperties": {
          "user": "novelai_reader"
        }
      }
    }
  ]
}
