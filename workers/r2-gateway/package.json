{
  "name": "dokushodo-r2-gateway",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:bindings": "vitest run --config vitest.bindings.config.ts",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy:dry-run": "wrangler deploy --env test --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.2",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
