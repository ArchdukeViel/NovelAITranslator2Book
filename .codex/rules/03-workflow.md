# Workflow

- Architecture and conventions — see `AGENTS.md`.
- Verification commands — see `AGENTS.md` "Verification Commands" section.
- Run focused tests first, not the full suite.
- Use `graphify query` before broad file searches.
- No Task tool, no subagents available — do all work directly.
- Pre-commit hook runs graphify update automatically.
