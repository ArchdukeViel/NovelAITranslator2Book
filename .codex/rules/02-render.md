# Render CLI

Render CLI installed at `render.exe`. Authenticated as `pelluakmal@gmail.com`.
Workspace: Dokushodo.

- `render services --output json` — list services
- `render postgres list --output json` — list Postgres databases
- `render logs <service-id> --output json` — view logs
- `render deploys list <service-id> --output json` — list deploys
- Use `--output json` or `--output yaml` for non-interactive output.
