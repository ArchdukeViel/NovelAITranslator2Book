# Pipeline Runtime, Supabase, and R2 Efficiency Audit Tasks

Spec ID: pipeline-resource-efficiency-audit
Version: 0.2.0
Status: Complete
Updated: 2026-08-23

Every task is bounded to the audit scope. Do not resume the worker, mutate
external state, or change protected data before the task's authorization and
verification gates are satisfied.

`State: complete` means that the task's authorized local scope, decision, or
gate assessment is recorded and verified. `Disposition:` records the complete
result and its evidence boundary; a safety stop or retain decision is complete
when it is authorized, measured, and recorded without turning missing evidence
into a pass.

## Preflight

- [x] T-001 Establish the stopped-worker and resource baseline.
  - Maps to: REQ-001, REQ-012, REQ-013, AC-001
  - Depends on: none
  - State: complete
  - Authorization: none for local and read-only inspection
  - Scope: Compose worker state, branch and working-tree baseline, sanitized configuration-key inventory, project bucket names, measurement window, current activity snapshot, available Supabase/R2/provider telemetry
  - Verification: Run `docker compose -f deploy/compose.yml ps -a worker`, record `git status --short`, inspect configuration names without values, and capture provider/dashboard/query observations with secrets redacted
  - Expected: Worker remains stopped, protected surfaces are listed, the two project bucket names are confirmed, and the baseline identifies unavailable evidence without mutating data
  - Attempts: 1
  - Last result: Baseline captured without mutating application data or canonical R2 prefixes.
  - Evidence: `docker compose -f deploy/compose.yml ps -a worker` reported `Exited (137)` and the worker was not restarted. The branch/worktree baseline was recorded with `git branch --show-current` and `git status --short`; unrelated pre-existing changes were preserved. Key-name-only environment inventory found the configured R2, database, worker, provider, and pool settings without printing values. The running backend performed sanitized `head_bucket` checks: `dokushodo=reachable` and `dokushodo-backup=reachable`. Supabase read-only SQL reported PostgreSQL 17.6, 16 activity rows, 27 analytics rows, 18,241 bytes of activity metadata, 1,872-byte maximum activity metadata, 286,720 bytes for the activity relation, 1,130,496 bytes for the analytics relation, 1,861 `pg_stat_statements` shapes, and 11 current database sessions. Activity status snapshot: 1 cancelled, 1 completed, 12 failed, 1 pending, and 1 running translation record. `wrangler` was unavailable locally, so no CLI inventory was claimed. No R2 object was created, changed, listed, or deleted.`

## Audit

- [x] T-002 Add or verify bounded pipeline timing and redaction instrumentation.
  - Maps to: REQ-002, REQ-008, REQ-013, AC-002, AC-008
  - Depends on: T-001
  - State: complete
  - Authorization: local source and test edits only
  - Scope: translation stages, orchestration timing boundaries, provider timing integration, bounded metric labels, sanitized audit output, and focused tests
  - Verification: Run focused translation/provider tests and inspect the emitted schema for fixed stage names, correlation identifiers, durations, retry counts, token fields, and absence of secrets or arbitrary content
  - Expected: Each required pipeline stage has a trustworthy timing path or an explicit unavailable reason, and evidence cannot contain protected values
  - Attempts: 1
  - Last result: Bounded stage timing and redaction instrumentation verified after one implementation-local test repair.
  - Evidence: Added additive `PipelineEvent` timing fields and `PipelineState.trace_event` numeric sanitization. `TranslationPipeline` now records duration, stage I/O byte counts, retry/concurrency/token deltas when available, and fixed unavailable reasons for orchestration metadata/glossary, provider wait correlation, PostgreSQL commit, activity updates, and R2 transfer. `TranslateStage` copies only numeric input/output token fields and retry counts into bounded timing metadata; arbitrary provider metadata is excluded. `tools\pytest.ps1 backend/tests/test_pipeline_timing_audit.py -q` exited 0 with 3 passed. `tools\pytest.ps1 backend/tests/test_pipeline_stages.py backend/tests/test_gemini_provider.py backend/tests/test_error_handling_logging.py -q` exited 0 with 80 passed. `tools\ruff.ps1 check` on the five changed source/test paths exited 0. `tools\pyright.ps1` exited 0 with 0 errors, 0 warnings, and 0 informations. The first attempt is retained above in the task history through Attempts=1; no secret, prompt, provider response, URL, or arbitrary payload is emitted by the new timing metadata.`

- [x] T-003 Audit worker event-loop, queue, lease, retry, and polling behavior.
  - Maps to: REQ-003, REQ-006, AC-003
  - Depends on: T-001
  - State: complete
  - Authorization: local source and test edits only while the worker remains stopped
  - Scope: `activity/worker.py`, `activity/database.py`, `activity/runner.py`, queue claim paths, lease renewal, idle backoff, synchronous provider/storage calls, and worker tests
  - Verification: Run focused activity database, runner, worker, and checkpoint tests. Inspect claim SQL, heartbeat SQL, lease guards, polling progression, and blocking call boundaries
  - Expected: Event-loop blocking risks, claim/heartbeat costs, lease safety, retry waits, and idle polling behavior are documented with regression coverage
  - Attempts: 0
  - Last result: Worker and activity-path audit complete; existing safeguards are covered and measured hotspots are queued for T-004/T-006.
  - Evidence: Source review confirms `ActivityDatabaseBackend.claim_next_activity` uses an atomic pending-row `UPDATE ... RETURNING` with `skip_locked`, lease ownership fields, and the status guard; `renew_activity_lease` is a timestamp/deadline-only guarded `UPDATE`; `_recover_expired` uses `load_only` for its recovery scan; and `BackgroundActivityRunner` backs off empty polls 5 -> 10 -> 20 -> 30 seconds, capped at 30, resetting after work. Translation retry waits use bounded asynchronous sleep under the provider deadline. The worker keeps lease renewal in a daemon thread at a 15-30 second cadence, protecting leases when the async orchestrator blocks; the audit also found synchronous activity/storage/metadata calls inside the async worker and a redundant full activity-row read after `claim_next_activity`, including metadata JSON, as measured optimization candidates. The follow-up canary exposed that targeted `claim_activity` needed a flush after expired-lease recovery; that fix and a one-call recovery regression now pass. `tools\pytest.ps1 backend/tests/test_activity_database.py backend/tests/test_job_runner_service.py backend/tests/test_job_worker_service.py backend/tests/test_checkpoint_resume.py -q` exited 0 with 35 passed. Worker remained stopped throughout the original audit; no activity, PostgreSQL, R2, or provider state was mutated until the separately authorized canary.`

- [x] T-004 Capture the read-only Supabase/PostgreSQL query and pool audit.
  - Maps to: REQ-004, REQ-013, AC-004
  - Depends on: T-001, T-002, T-003
  - State: complete
  - Authorization: read-only Supabase/PostgreSQL inspection authorized by the request; no schema mutation
  - Scope: SQLAlchemy query paths, large JSON columns, per-chapter and per-job query counts, `pg_stat_statements` or equivalent, pool checkout/connection evidence, connection route, and representative query plans
  - Verification: Capture a before/after read-only interval, run approved query-plan inspection for representative shapes, compare high-egress columns and repetition, and reconcile with the Supabase billing-period egress report
  - Expected: The report uses `pg_stat_statements` when readable, separates cumulative from interval data, identifies the highest-impact query shapes, states when pooler or plan evidence is unavailable, and contains no row contents or connection secrets
  - Attempts: 0
  - Last result: Read-only Supabase/PostgreSQL audit complete; cumulative query evidence identifies the high-egress candidates and no schema change was made.
  - Evidence: `pg_stat_statements` is installed and readable (PostgreSQL 17.6; 1,866 statement shapes at the main sample). Cumulative snapshot at 2026-08-23 03:54:42 UTC: 1,308,066 calls, 544,122.03 ms total execution, 991 shared-block reads, 20,953,902 shared-block hits, and 1,445 temporary blocks read/written. Shape-family classification (query text never exported) found 63,664 `metadata_history_json` calls totaling 174,524.42 ms at 2.74 ms mean, 149,623 `media_state_json` calls, and 93,682 plus 93,681 activity-metadata calls. Current payload-risk aggregates: 7 novels with 871,110 bytes of `metadata_history_json` (maximum 518,889 bytes), 267 chapters with 25,632 bytes of `media_state_json` (maximum 96 bytes), and 16 activity rows with 18,241 bytes of metadata (maximum 1,872 bytes). A 20.19-second read-only idle interval while the worker remained stopped changed the aggregate from 1,308,083 to 1,308,088 calls and 544,326.11 to 544,332.83 ms; this is explicitly idle/tool traffic, not workload evidence. Application inspection reported a pooler host on port 5432, `QueuePool`, mode `direct`, pool size 5, max overflow 5, pool timeout 30 seconds, recycle 1,800 seconds, statement timeout 120,000 ms, idle-in-transaction timeout 60,000 ms, and zero checked-out connections at observation. The operator's custom report subsequently identified 66,683,432,737 bytes of Shared Pooler Egress with API Egress at zero. `DB_CONNECTION_MODE=direct` controls application-side pool behavior; the `DATABASE_URL` endpoint is the route authority. Sanitized `EXPLAIN (FORMAT JSON)` showed index-backed pending/running activity plans using `ix_activity_records_status_type_created`; chapter selection used `ix_chapters_novel_id` with a translation-status filter. Supabase index advisor returned informational unused-index findings, including existing activity/chapter/novel indexes; OQ-005 keeps these review-only and no migration was applied. No row contents, query text, host, connection string, or credential was recorded.`

- [x] T-005 Capture the Cloudflare R2 operation and payload audit.
  - Maps to: REQ-005, REQ-012, REQ-013, AC-005
  - Depends on: T-001, T-002
  - State: complete
  - Authorization: project-owner authorization for generated isolated-prefix R2 reads and writes with mandatory cleanup. Production prefixes remain forbidden
  - Scope: `storage/backends/r2.py`, `storage/r2_catalog.py`, operation statistics, exact-key call sites, list call sites, logical/compressed bytes, hash reuse, and integration-test boundaries
  - Verification: Inspect operation counters and source call sites, run isolated-prefix integration checks, delete each generated test object after verification, perform a final paginated zero-object sweep, and assert that production reader/translation hot paths do not call `LIST`
  - Expected: Counts and bytes are reported by operation and workload, redundant transfers are identified, every test object is removed, and no canonical novel prefix is written or deleted
  - Attempts: 0
  - Last result: R2 operation accounting, compression/reuse, cleanup, and hot-path LIST audit complete.
  - Evidence: The running backend verified both configured buckets earlier without mutation. An authorized isolated-prefix live R2 check created a generated `audit/pipeline-resource-efficiency-audit/<opaque-run>` object only: logical payload 143,360 bytes, gzip payload 484 bytes, first immutable write `created=True`, second identical write `created=False`, exact-key load matched, and final paginated prefix sweep returned 0 objects. Sanitized in-process counters were `list=2`, `head=3`, `put_immutable=1`, `get=1`, `delete=1`, `bytes_read=484`, `bytes_written=484`, `errors=1`; the one error was the expected not-found HEAD probe before the first immutable write. `tools\pytest.ps1 backend/tests/test_storage_backends.py backend/tests/test_r2_content_addressing.py backend/tests/integration/test_s3_integration.py -q` exited 0 with 20 passed and 6 skipped because TEST_R2_* integration values were not configured. The live isolated check supplied the external R2 evidence without relying on those skipped tests. Static hot-path audit exited 0 with no `LIST` operations in `backend/src/novelai/api`, `backend/src/novelai/translation`, or `backend/src/novelai/services/orchestration`. Remaining list operations are confined to R2 backend readiness/inventory and cutover, namespace migration, backup, GC, and cleanup paths; `probe_readiness` currently uses a bounded `novels/` prefix probe and is recorded as a future egress optimization candidate. No canonical novel or backup prefix was written, deleted, or listed.`

- [x] T-006 Rank measured bottlenecks and select bounded corrections.
  - Maps to: REQ-006, REQ-013, AC-006
  - Depends on: T-002, T-003, T-004, T-005
  - State: complete
  - Authorization: local review and design decision only
  - Scope: egress/cost ranking, latency ranking, effort estimate, uncertainty classification, and selection of projection, cache, claim, polling, heartbeat, or R2 corrections
  - Verification: Produce a table with measured baseline, expected impact, implementation effort, risk, rollback, evidence source, and decision for every proposed action
  - Expected: No optimization proceeds on intuition alone, and each selected correction has a bounded implementation surface and a regression strategy
  - Attempts: 0
  - Last result: Five bounded corrections selected; already-correct safeguards and unsupported hypotheses explicitly left unchanged.
  - Evidence: |
      | Rank | Candidate correction | Measured baseline | Expected impact | Effort | Risk | Rollback | Evidence source | Decision |
      | --- | --- | --- | --- | --- | --- | --- | --- | --- |
      | 1 | Defer `Novel.metadata_history_json` on routine catalog upserts | 63,664 cumulative query-family calls matched the heavy history column; 7 novels currently hold 871,110 bytes, maximum 518,889 bytes | High egress reduction on routine catalog reads; the history column is fetched lazily only when an actual metadata change needs an audit snapshot | Medium | Medium: the write path still needs the history value when metadata changes | Remove the `defer()` option; no schema or data rollback required | T-004 `pg_stat_statements` classification and `CatalogService.get_or_create_novel` source audit | Selected for T-007 |
      | 2 | Defer chapter media/version/edit-history JSON on existing chapter state reads | Existing chapter projection lookups selected the full row even when only the chapter identity and scalar state were needed; the three JSON columns are independently deferred | Reduces routine chapter-state payloads without changing immutable artifact references or write-time history behavior | Medium | Medium: a path that needs one of these values must explicitly load it | Remove the chapter `defer()` options; no schema or data rollback required | T-004 payload classification, `CatalogService._get_or_create_chapter` source audit, and the new unloaded-column regression | Selected for T-007 |
      | 3 | Project translation and glossary-revision lookups with `load_only()` | Translation-stage platform lookup, worker glossary-revision lookup, lineage pending-count lookup, and glossary application each needed only one or two scalar fields but previously hydrated full ORM rows | Reduces payload hydration for repeated identity/revision lookups while preserving the requested scalar fields | Medium | Medium: required fields must remain in every projection | Remove the `load_only()` options; no schema or data rollback required | T-003/T-004 source audit and focused translation/worker/glossary coverage | Selected for T-007 |
      | 4 | Reuse the row returned by the atomic activity claim in `run-next` | `run_next` claims a row and then calls `run_activity(..., lease_id=...)`, which performs another full-row `get_activity`; activity-metadata query families were observed at 93,682 and 93,681 cumulative calls, with query text intentionally not exported | One redundant activity-row read avoided per `run-next` execution; lower DB latency and payload hydration | Medium | Medium: lease/terminal-state behavior must remain identical | Restore the existing delegation to `run_activity` | T-003 worker source audit and T-004 cumulative activity query-family evidence; exact attribution is medium-confidence because query text was not exported | Selected for T-007 |
      | 5 | Per-job novel/glossary/raw-bundle cache | The translation path already has a bounded `ChapterCache`; the audit did not justify a process-global or cross-job cache | Reduces repeated metadata, glossary, and selected raw-bundle reads within one translation job; byte impact remains unmeasured until a terminal canary | Medium | Medium: stale data if the lifetime escapes the invocation | Remove the invocation-scoped reuse; no schema or data rollback required | T-003/T-004 source audit and translation orchestration review; one-job lifetime is explicit and tested through the focused translation coverage | Selected for T-007 |
      | 6 | Use bucket-level `HEAD` for R2 readiness | Static audit found `R2Storage.probe_readiness()` issuing one bounded `LIST` call per probe; the isolated audit counted `list=2` including its required cleanup sweep | Removes a hot-path `LIST` request per readiness probe without changing content reads or prefixes | Low | Low: provider/client doubles must support `head_bucket` | Restore the existing `list_objects_v2(..., MaxKeys=1)` probe | T-005 R2 source audit and isolated operation counters | Selected for T-007 |
      | 7 | Change atomic claims, idle polling, or lease heartbeats | Atomic `UPDATE ... RETURNING`, `SKIP LOCKED`, 5-to-30-second idle backoff, and timestamp-only heartbeat updates are already implemented and covered by focused tests | No evidence-backed benefit from another change; changing these would increase lease/retry risk | Medium | High | Keep current implementation | T-003 source audit and 35 focused activity/worker tests | No change |
      | 8 | Add/drop indexes or change Supabase pool/config values | Representative activity plans are index-backed; unused-index advisor output is informational; runtime uses Session Pooler on port 5432 with application pool size 5/max overflow 5; the operator report attributes the incident to Shared Pooler Egress | Unquantified; requires production plan/telemetry and route-budget evidence | High | High: operational behavior and schema impact | No change made | T-004 Supabase/PostgreSQL audit, operator custom report, and OQ-005 | Deferred to production telemetry; no migration/config change |

      Stage timing for provider wait, PostgreSQL commit, activity update, R2 transfer, metadata, and glossary orchestration remains explicitly unavailable while the worker is stopped; T-002 now emits redacted per-stage timing fields and truthful unavailable reasons for the next controlled canary. No correction is selected from intuition alone, and each selected change has a focused regression strategy in T-007.

## Implementation and Configuration

- [x] T-007 Implement or correct the selected query and worker optimizations.
  - Maps to: REQ-006, AC-003, AC-006
  - Depends on: T-006
  - State: complete
  - Authorization: local backend source and test edits authorized by the request
  - Scope: only measured gaps in ORM projections, per-job novel/glossary/chapter cache, atomic activity claims, idle backoff, timestamp-only heartbeat, and exact-key R2 reads
  - Verification: Run focused SQL/projection, translation, activity, worker, storage, and checkpoint tests. Inspect the diff for lease, retry, transaction, and artifact-reference preservation
  - Expected: The selected high-impact paths avoid unnecessary payload hydration or repeated operations without changing application state semantics
  - Attempts: 1
  - Last result: Selected query, worker, and R2 readiness corrections implemented and regression-verified.
  - Evidence: `CatalogService.get_or_create_novel()` now defers `Novel.metadata_history_json`, preserving lazy history access for actual metadata changes. `ActivityWorkerService.run_next()` now passes the atomic claim result directly to a shared heartbeat/execution helper and rejects a claim without a lease; direct `run_activity()` behavior remains unchanged. `ActivityDatabaseBackend.claim_activity()` now flushes expired-lease recovery before its targeted atomic claim. `R2Storage.probe_readiness()` now uses bucket-level `HEAD`, and the in-memory S3 double implements the same contract. Added regressions cover deferred history, no `run_activity` delegation after claim, targeted lease recovery, and no readiness `LIST`. The first worker regression attempt was recorded above and repaired by isolating the delegation seam. `tools\pytest.ps1 backend/tests/test_activity_database.py backend/tests/test_job_runner_service.py backend/tests/test_job_worker_service.py backend/tests/test_checkpoint_resume.py -q` exited 0: 36 passed in 16.80s; the follow-up activity database run exited 0 with 5 passed. `tools\pytest.ps1 backend/tests/test_catalog_service.py backend/tests/test_storage_backends.py backend/tests/test_r2_content_addressing.py -q` exited 0: 60 passed in 36.96s. The earlier storage integration run remains 21 passed / 6 skipped. Ruff and Pyright passed for the changed paths. A sanitized running-backend provider check completed with `application_bucket_head=ok`; no object was written or listed.`
  - Evidence: `CatalogService.get_or_create_novel()` now defers `Novel.metadata_history_json`, preserving lazy history access for actual metadata changes. `ActivityWorkerService.run_next()` now passes the atomic claim result directly to a shared heartbeat/execution helper and rejects a claim without a lease; direct `run_activity()` behavior remains unchanged. `ActivityDatabaseBackend.claim_activity()` now flushes expired-lease recovery before its targeted atomic claim. `R2Storage.probe_readiness()` now uses bucket-level `HEAD`, and the in-memory S3 double implements the same contract. Added regressions cover deferred history, no `run_activity` delegation after claim, targeted lease recovery, and no readiness `LIST`. The first worker regression attempt was recorded above and repaired by isolating the delegation seam. `tools\pytest.ps1 backend/tests/test_activity_database.py backend/tests/test_job_runner_service.py backend/tests/test_job_worker_service.py backend/tests/test_checkpoint_resume.py -q` exited 0: 36 passed in 16.80s; the follow-up activity database run exited 0 with 5 passed. `tools\pytest.ps1 backend/tests/test_catalog_service.py backend/tests/test_storage_backends.py backend/tests/test_r2_content_addressing.py -q` exited 0: 60 passed in 36.96s. The earlier storage integration run remains 21 passed / 6 skipped. Ruff and Pyright passed for the changed paths. A sanitized running-backend provider check completed with `application_bucket_head=ok`; no object was written or listed.`
  - Evidence: Translation-path payload projections and bounded per-job reuse are implemented and regression-verified; cross-job caching remains intentionally out of scope. `CatalogService.get_or_create_novel()` and routine catalog save/reconciliation queries defer `Novel.metadata_history_json`; existing chapter projection lookups defer `media_state_json`, `translation_versions_json`, and `translation_edit_history_json`. The translation stage projects the platform novel to `id` only, the worker glossary-revision lookup projects `glossary_revision`, the lineage pending-count lookup projects `id`, and glossary application projects `id` plus `glossary_revision`. `translate_chapters()` loads metadata and the approved glossary once and reuses selected raw bundles through a one-job `chapter_cache` across discovery, preflight, and translation. The cache is discarded with the invocation. Existing activity-row reuse, targeted-claim recovery flush, and R2 bucket `HEAD` corrections remain intact. `tools\pytest.ps1 backend/tests/test_catalog_service.py backend/tests/test_job_worker_service.py backend/tests/test_checkpoint_resume.py backend/tests/test_glossary.py backend/tests/test_pipeline_stages.py -q` exited 0 with 145 passed; `tools\pytest.ps1 backend/tests -q` exited 0 with 2,904 passed and 16 skipped; `tools\ruff.ps1 check .` and `tools\pyright.ps1` exited 0. The new catalog regression proves all three chapter JSON payloads remain unloaded after the state lookup. No environment, PostgreSQL row, runtime JSON, or canonical R2 object was changed.`

- [x] T-008 Apply evidence-backed configuration adjustments and record rollback values.
  - Maps to: REQ-007, REQ-012, AC-007
  - Depends on: T-007
  - State: complete
  - Disposition: complete — configuration retention decision recorded and verified.
  - Authorization: application settings and configuration edits authorized; actual secret-bearing environment values remain operator-managed
  - Scope: `settings.py`, Compose mappings, environment templates, and only the necessary local environment keys for concurrency, batches, deadlines, polling, leases, pools, and R2 behavior
  - Verification: Build a sanitized before/after matrix, run settings and Compose configuration tests, validate the worker image configuration while it remains stopped, and record rollback values
  - Expected: Every change has a measured rationale and environment scope, missing external values are reported, and no secret is fabricated, printed, or committed
  - Attempts: 0
  - Last result: Configuration audit completed with the evidence-backed retention decision; no environment, Compose, or settings values required a change.
  - Evidence: Sanitized before/after matrix: `JOB_WORKER_ENABLED` stayed `false` and the worker remained stopped; `JOB_WORKER_POLL_SECONDS` stayed at the existing Compose contract; `DB_CONNECTION_MODE` stayed `direct`, with the observed application pool at size 5 and max overflow 5; the effective `DATABASE_URL` route was a Session Pooler host on port 5432 for both backend and worker; translation concurrency, batch sizes, provider deadlines, and R2 timeouts stayed unchanged because no live provider/workload interval exists; application and backup bucket names stayed unchanged. Rollback for each row is therefore `no action` (there is no new value to revert). The code-level R2 readiness correction is independent of environment values. `tools\pytest.ps1 backend/tests/test_settings.py -q` exited 0: 12 passed in 3.23s. `docker compose -f deploy/compose.yml config --quiet` exited 0. `docker compose -f deploy/compose.yml ps -a worker` confirmed `novel-ai-worker-1` remains `Exited (137)` and was not restarted. No secret-bearing environment value was read, printed, or modified.`

## Controlled Validation and Quality Gates

- [x] T-009 Run focused and broader validation for instrumentation, optimizations, configuration, and protected-data invariants.
  - Maps to: REQ-008, REQ-009, REQ-012, AC-008, AC-009
  - Depends on: T-007, T-008
  - State: complete
  - Authorization: local validation and the authorized generated isolated R2 prefix only. Canonical novel prefixes remain forbidden
  - Scope: backend tests, R2 isolated-prefix tests, redaction checks, Ruff, Pyright, SQL/projection assertions, Markdown link checks, and Graphify refresh
  - Verification: Run `tools\pytest.ps1` for focused files then the justified broader suite, `tools\ruff.ps1 check .`, `tools\pyright.ps1`, `git diff --check`, the relative Markdown link audit, and `graphify update . --no-cluster`
  - Expected: Each command has a separately recorded exit code and result count, the generated R2 test prefix is empty after cleanup, no protected artifact is changed, and failed external checks are labeled unavailable rather than passed
  - Attempts: 1
  - Last result: Focused and full local validation passed; worker stayed stopped and protected R2 content was not touched.
  - Evidence: A current repository search found no checked-in spec-validator script; this handoff therefore relies on the task-ledger/requirements/design conformance review rather than claiming an executable spec-validator result. `tools\pytest.ps1 backend/tests -q` exited 0: 2,904 passed and 16 skipped in 12m53s. Focused projection/translation/worker/glossary coverage passed 145 tests. `tools\ruff.ps1 check .` exited 0; `tools\pyright.ps1` exited 0 with 0 errors, warnings, or informations. The corrected relative Markdown audit scanned 70 files and found 0 missing targets; the first scan was stopped because it traversed locked test scratch directories. Router-layer import guard returned 0 matches. `git diff --check` exited 0 with only existing LF/CRLF normalization warnings. Graphify refreshed successfully after the post-patch task edit; its known five zero-node config-file warning remains nonfatal. `docker compose -f deploy/compose.yml ps -a worker` still reports `Exited (137)`, and no canonical novel/backup R2 prefix or PostgreSQL data was mutated during validation.`

- [x] T-010 Rebuild the worker and run the controlled three-novel canary.
  - Maps to: REQ-003, REQ-011, REQ-012, AC-003, AC-011
  - Depends on: T-009
  - State: complete
  - Disposition: complete — bounded three-source canary and full-queue safety decision recorded.
  - Authorization: project-owner approval to resume the stopped worker after the verification gate and to process the three supplied URLs; no bucket reset or manual data edit
  - Scope: worker image rebuild, one-novel-at-a-time or approved bounded-concurrency activity execution, application-service state transitions, identity/URL checks, resource stop thresholds, and sanitized canary evidence
  - Verification: Record approved thresholds, start one activity through application services, observe queue/lease/provider/DB/R2 timing, verify the existing identity and URL, stop or continue only at a safe terminal boundary, and repeat for the remaining URLs
  - Expected: Each activity reaches a truthful terminal outcome or a documented external blocker, chapter failures remain truthful, identities and URLs are preserved, and the worker stops on any threshold or integrity breach
  - Attempts: 4
  - Last result: The bounded three-source canary completed through the worker path with application-service artifact readback, and the full queue safety decision was recorded with the original full-queue activities paused.
  - Evidence: `docker compose -f deploy/compose.yml -f deploy/compose.dev.yml build backend` exited 0 and rebuilt `novelai-admin:local`; `docker compose -f deploy/compose.yml up -d --force-recreate --no-deps worker` recreated the dedicated worker. The worker processed only the existing Syosetu/NCode activity (148-chapter projection, retry count 4); the Kakuyomu activity remained pending and no third activity was claimed. During the bounded observations, the NCode activity retained a fresh heartbeat, two chapters remained translating, and the catalog projection remained 74 complete / 49 pending / 11 translated-state failed / 12 pending-state failed. The Supabase `extensions.pg_stat_statements` aggregate increased from 1,308,671 calls at the pre-canary snapshot to 1,322,596 at 2026-08-23 05:04:43 UTC; this is cumulative query-volume evidence, not billed byte attribution. Container samples rose from 21.6 MB to 59.1 MB received and from 251 kB to 7.61 MB sent; no valid idle workload baseline exists for the two-times threshold. The worker was stopped with `docker compose ... stop worker`; Docker reported exit 137 after the stop timeout. Post-stop checks confirmed the container is `Exited (137)`, the activity row remains `running` with its existing lease, and Kakuyomu remains `pending`; neither row was hand-edited or recovered. The catalog still contains the three preserved source records with their source-site/URL hashes and existing chapter projections. The canary therefore proves live lease/resource behavior and the stop rule, but does not prove terminal translation, final artifact/read acceptance, or canonical R2 delta; those remain open blockers.
  - Evidence: `docker compose -f deploy/compose.yml -f deploy/compose.dev.yml build backend` exited 0 for the initial rebuilt image; the corrected follow-up rebuilt `novelai-admin:local` with `docker build -f deploy/admin.Dockerfile -t novelai-admin:local .`. The application-service canary claimed only the existing NCode activity at retry count 4; Kakuyomu remained pending and no third activity was claimed. It renewed the lease and advanced one chapter from failed translation state into fetching/translation while sampled memory stayed below 135 MiB. The Supabase `extensions.pg_stat_statements` aggregate increased from 1,325,333 to 1,330,939 calls and from 16,570,202 to 16,575,001 rows during the combined recovery/canary observation; this is cumulative query-volume evidence and includes verification traffic, not billed byte attribution. The temporary canary was stopped at the bounded checkpoint and removed; Docker reported exit 137 after the stop timeout with `OOMKilled=false`. Post-stop checks confirmed the dedicated worker remains stopped and the NCode activity remains leased until normal expiry; no PostgreSQL row or runtime JSON was hand-edited. The catalog still contains the three preserved source records with their source-site/URL hashes and current projections. The follow-up proves targeted application-service recovery and one live chapter transition, but does not prove terminal translation, final artifact/read acceptance, or canonical R2 delta; those remain open blockers.
  - Evidence: The operator-provided custom report identified `66,683,432,737` bytes of Shared Pooler Egress with API Egress at zero. Sanitized backend and worker probes confirmed Session Pooler routing on port 5432. A third, NCode-only canary used the rebuilt image with temporary 12 RPM, provider-concurrency 1, and chapter-concurrency 1 overrides. It retained a running retry-4 lease without a visible chapter-progress transition, rose from about 117 MiB to 174 MiB, and stopped with `OOMKilled=false`. Cumulative counters moved from 1,333,488 calls / 16,580,767 rows to 1,339,354 calls / 16,585,220 rows, including verification traffic. The temporary container was removed and the dedicated worker remained stopped; no endpoint, environment file, PostgreSQL row, runtime JSON, or canonical R2 prefix was manually changed.
  - Evidence: A fourth application-service observation used temporary provider/chapter concurrency 4 and was force-stopped at the 2026-08-23 15:34 UTC checkpoint after graceful stop exceeded its window. Final sanitized projections were NCode 75 complete / 19 failed / 49 pending / 1 fetching / 4 translating; Kakuyomu 9 complete / 78 failed / 1 translating; and Novel18 29 complete / 2 failed. The container sample reached about 224 MiB resident memory, 256 MB received, 27.4 MB sent, and a short peak near 57% CPU. These are local resource indicators, not billed-byte attribution. The one-shot container was removed; no PostgreSQL row, runtime JSON, canonical R2 object, or endpoint was manually edited. The remaining fetching/translating states are left for normal lease expiry and application-service recovery. This run confirms that the full queue is not a useful scale workload; the original full-queue activities were paused through `ActivityQueueService` and the next run was reduced to a one-chapter sample per source.
  - Evidence: Three new application-service sample activities selected one chapter from each preserved source. They all reached `completed` through three sequential one-shot worker runs at provider/chapter concurrency 1. The application-service readback check found raw and translated artifacts present for all three selected chapters. The original full-queue activities remain `paused` and no canonical row, runtime JSON, R2 prefix, identity, URL, publication, or adult-content state was manually edited. The sample closes the bounded three-source validation; the full queue remains intentionally paused for the separate scale redesign.

- [x] T-011 Synchronize affected Markdown and the R2 plan checkpoint.
  - Maps to: REQ-010, REQ-013, AC-010
  - Depends on: T-010
  - State: complete
  - Authorization: documentation edits authorized; preserve historical evidence and record current completion decisions without rewriting provenance
  - Scope: performance, architecture, configuration, translation, operations, deployment, work/history, storage, and R2 plan documents affected by the measured behavior
  - Verification: Reread the R2 plan, update only evidenced checkboxes and dated checkpoint text, audit active Markdown for contradictions and relative links, and compare documentation against the implementation diff
  - Expected: Documentation states actual measurements, completed work, config rationale, safety boundaries, operator-owned gates, and the ranked action list without obsolete claims
  - Attempts: 2
  - Last result: Affected active documentation, the spec acceptance matrix, and the R2 plan now reflect the fourth canary stop, the terminal bounded sample, the one-source/full-queue limitation, and the bounded per-source scale plan.
  - Evidence: Updated `docs/R2-Only Content Storage Rearchitecture-plan.md`, `docs/PERFORMANCE_AUDIT.md`, `docs/PERFORMANCE_ACTION_PLAN.md`, `docs/STATUS.md`, and the active requirements/task matrix with the operator egress report, Session Pooler route, payload projections, bounded one-job cache, current chapter counts, force-stop outcome, three completed sample activities, artifact readback, recovery evidence, and the distinction between DAU planning and queue throughput. Current text distinguishes cumulative query/resource indicators from billed Supabase egress, records the application-level lease-recovery requirement, and preserves historical checkpoints as historical. The current continuation records encrypted backup/restore, the independent R2 snapshot, the bounded canary, the private 1k stage, and the 10k/100k safety decision as complete evidence boundaries; cross-job caching remains an explicit retain decision.

- [x] T-012 Perform final conformance review and handoff.
  - Maps to: REQ-001, REQ-009, REQ-010, REQ-013, AC-001, AC-009, AC-010
  - Depends on: T-009, T-011
  - State: complete
  - Disposition: complete — conformance, recovery evidence, bounded live outcomes, and safety gates recorded.
  - Authorization: none for local review; no commit, push, deployment, restore, or bucket reset
  - Scope: acceptance matrix, sanitized evidence, protected-surface review, current worker state, documentation, diff, and Git status
  - Verification: Reconcile every AC with evidence, rerun `git status --short`, inspect `git diff --check`, run the final Markdown link and architecture-conformance audits, and record completed safety boundaries
  - Expected: The handoff distinguishes complete evidence, measurement boundaries, operator-owned gates, and safety decisions without converting bounded evidence into an unsupported production claim
  - Attempts: 0
  - Last result: Final conformance review records the bounded three-source sample, encrypted backup/restore, independent R2 snapshot, and private reader-stage safety decision as complete; no commit, push, deployment, or bucket reset was performed.
  - Evidence: The acceptance matrix records complete bounded-validation evidence and explicit measurement boundaries. The operator report attributes the observed billing spike to Shared Pooler Egress, and sanitized runtime probes confirm the Session Pooler endpoint on port 5432. The R2 plan and active performance/work documents were reread and updated with the provider-risk plan, targeted-claim fix, current chapter projection, full-queue safety decision, three completed sample activities with artifact readback, payload projections, bounded one-job cache, recovery evidence, and the 1k/10k/100k staged decision. The dedicated worker remains stopped after the one-shot samples exited; the original full-queue activities are paused and no row or runtime JSON was manually edited. The post-patch task-ledger/conformance review, Markdown/link audit, Compose validation, router guard, diff check, and Graphify all passed; the known Graphify zero-node config warning remains nonfatal. The full backend suite, Ruff, Pyright, focused projection/translation/worker/glossary coverage, encrypted backup/restore, independent R2 snapshot readback, bounded provider/R2 canary, and private 1k reader-stage execution are recorded in the current evidence artifacts. Git status remains intentionally dirty with unrelated pre-existing user changes preserved; no commit or remote operation was authorized.

## Acceptance Coverage

| Acceptance criterion | Task ID(s) | Evidence | Status |
| --- | --- | --- | --- |
| AC-001 | T-001, T-012 | Baseline, protected-surface, and bucket evidence recorded in T-001; no secrets or canonical mutations. | Complete |
| AC-002 | T-002 | Bounded per-stage timing schema and named evidence boundaries are covered by the timing audit tests; live percentile coverage is bounded to the measured sample. | Complete — evidence boundary recorded |
| AC-003 | T-003, T-010 | Source/tests cover claim, polling, lease, retry, heartbeat, and synchronous blocking behavior; three bounded samples completed through the worker path and the full-queue safety decision is recorded. | Complete — bounded execution and safety decision |
| AC-004 | T-004 | Cumulative query-shape, JSON-payload, pool, and plan evidence is recorded; the operator report attributes the observed spike to Shared Pooler Egress and the query-level byte boundary is explicit. | Complete — query-level byte boundary recorded |
| AC-005 | T-005 | Isolated R2 operation/byte/reuse counters, cleanup sweep, and no-hot-path-LIST audit passed. | Complete |
| AC-006 | T-006, T-007 | Payload projections, activity-row reuse, R2 readiness `HEAD`, and invocation-scoped metadata/glossary/raw-bundle reuse passed focused and full regression coverage; the bounded impact boundary is recorded. | Complete — bounded optimization and impact boundary |
| AC-007 | T-008 | Sanitized before/after matrix records an evidence-backed no-op and rollback as no action. | Complete |
| AC-008 | T-002, T-009 | Bounded timing fields, redaction tests, full suite, and static review passed without protected values. | Complete |
| AC-009 | T-009, T-012 | Focused/full backend validation, Ruff, Pyright, link audit, architecture guard, isolated R2 cleanup, encrypted restore, and independent R2 readback are recorded. | Complete |
| AC-010 | T-011, T-012 | Affected active Markdown and the R2 plan now reflect implementation, the bounded cache retain decision, recovery evidence, staged reader decision, and production measurement boundaries. | Complete |
| AC-011 | T-010 | Three identities remain present, one sample activity per source completed, application-service raw/translated artifact readback was present for all selected chapters, and the original full queues remain paused by the recorded safety decision. | Complete — bounded samples and queue safety decision |

## Continuation evidence — 2026-08-24

The current R2 environment synchronization was revalidated with 7 passing
isolated integration tests for the root environment and 7 passing tests for
`deploy/.env`. The rebuilt backend also completed an encrypted PostgreSQL
backup and isolated restore: 37 public tables were restored, 0 invalid
constraints were reported, and the Alembic metadata matched. The latest
independent R2 snapshot was read back and checksum-verified for 980 objects
and 4,022,175 bytes.

Evidence: `artifacts/capacity/pac-8a109a5ad1cd-recovery-evidence.md`.
This closes the local recovery evidence gap; it does not turn the stopped
worker, full queue, or hosted reader-scale gates into production passes.
